<?xml version="1.0" encoding="UTF-8"?>
<!-- 맵퍼로 xml과 연결시킬 dao는 이름이 같아야 함 단, 맵퍼를 사용하려면 마이바티스 DOCTYPE 임포트가 필요 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bob.stepy.dao.MemberDao">

	<select id="duplicationCheck" parameterType="String" resultType="int">
	SELECT COUNT (M_ID) FROM M WHERE M_ID = #{tempid}
	</select>

	<select id="memberInsert" parameterType="com.bob.stepy.dto.MemberDto">
	INSERT INTO M VALUES (#{m_id}, #{m_pwd}, #{m_email}, #{m_name}, #{m_nickname}, #{m_birth}, #{m_gender}, #{m_phone}, #{m_addr})
	</select>
	
	<select id="getMemeberInfo" parameterType="String" resultType="com.bob.stepy.dto.MemberDto">
	SELECT * FROM M WHERE M_ID=#{m_id}
	</select>

	<select id="getEncryptizedPass" parameterType="String" resultType="String">
	SELECT M_PWD FROM M WHERE M_ID = #{m_id}
	</select>
</mapper>